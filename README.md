# Insertion-Sort

•	Implemented the insertion sort algorithm in Bash, sorting command-line arguments into ascending order
•	Handled array indexing and element shifting manually using nested loops, without relying on built-in sort utilities
